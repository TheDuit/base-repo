# Copie para .env.aws para ativar um profile AWS isolado do projeto.
# Nao coloque secrets neste arquivo; credenciais ficam em ~/.aws/credentials ou SSO.

export AWS_PROFILE=base-system
export AWS_REGION=us-east-1
export AWS_DEFAULT_REGION=us-east-1
